package mrfinger.gothicgamemod.util;

public class RestoreValues {
	/*
	private EntityLivingBase entity;
	private GGMEntityLivingBase statsEntity;
	
	private Map<String, Float> restoreMap = new HashMap<String, Float>();
	
	private Map<String, Float> restorePercentMap = new HashMap<String, Float>(); 
	
	private float healValue;	
	
	public RestoreValues() {
		
	}
	
	public RestoreValues(String stat, float value, float percentValue) {
		this.restoreMap.put(stat, value);
		if (percentValue > 100.0F) percentValue = 100.0F;
		this.restorePercentMap.put(stat, percentValue);		
	}
	
	public RestoreValues setRestoreValues (String stat, float value, float percentValue) {
		this.restoreMap.put(stat, value);
		if (percentValue > 100.0F) percentValue = 100.0F;
		this.restorePercentMap.put(stat, percentValue);	
		return this;
	}
	
	public List<String> getKeys() {
		return new ArrayList(this.restoreMap.keySet());
	}
	
	public float getRestoreValue (String stat) {
		return this.restoreMap.get(stat);
	}
	
	public float getRestorePercentValue (String stat) {
		return this.restorePercentMap.get(stat);
	}	*/
		
}
